# Yapılacak İşler (TODO)

- [ ] `login.ejs` dosyasındaki iç içe geçmiş `<form>` yapılarını temizle ve dil/tema formlarını dışarı taşı.
- [ ] `src/app.js` içerisine ters proxy uyumluluğu için `app.set('trust proxy', 1);` ekle.
- [ ] Yerel değişiklikleri (`git status` ile görünenler + yeniler) commit et ve git push yap.
- [ ] Kullanıcıya sunucu tarafında yapması gerekenleri (git pull, .env güncelleme, yeniden başlatma) açıkla.
