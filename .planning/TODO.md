# Yapılacak İşler (TODO) - Görsel Optimizasyonu

- [ ] Python betiği `compress_images.py` oluştur ve çalıştır:
  - [ ] PNG görsellerini 400x400 WebP formatına dönüştür.
  - [ ] Eski PNG dosyalarını sil.
- [ ] `src/config/db.js` içindeki seeder görsel yollarını `.png`'den `.webp`'ye çevir.
- [ ] Mevcut SQLite veritabanındaki ürün görsel uzantılarını güncelle.
- [ ] Eski `.png` dosyalarını `git rm` ile kaldır, yeni `.webp` dosyalarını `git add -f` ile ekle.
- [ ] Değişiklikleri test et ve `git push` yap.
